{
  "name": "settings-service",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "client:install": "cd client && npm install",
    "client:build": "cd client && npm run build",
    "start": "npm run client:build && npm run dev",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
